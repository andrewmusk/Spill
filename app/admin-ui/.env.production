# Production Environment Variables
# These are safe to commit - they're public values that get embedded in the client bundle

# Backend API URL (production)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Clerk Publishable Key (production)
# This is safe to commit - publishable keys are designed to be public
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...

# Note: CLERK_SECRET_KEY should NOT be here
# Set it in your deployment platform's environment variables (Vercel, Railway, etc.)
